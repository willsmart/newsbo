.app
    %h2
        Welcome to ${name}
    %h4
        These are the users...
    .users-model-child{variant: "${state.selectedUser==id ? 'selectedtablerow' : (posts && posts.length ? 'tablerow' : 'nopoststablerow')}"}
